<UserControl
    x:Class="SAE201.Usercontrol.ToutesLesCommandes"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SAE201.Usercontrol"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="clr-namespace:SAE201.Classes"
    d:DesignHeight="700"
    d:DesignWidth="1000"
    Background="White"
    mc:Ignorable="d">


    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="45*" />
            <RowDefinition Height="41*" />
            <RowDefinition Height="244*" />
        </Grid.RowDefinitions>

        <TextBlock
            Width="296"
            Margin="0,10,0,10"
            HorizontalAlignment="Center"
            FontSize="28"
            FontWeight="Bold"
            Text="Toutes les commandes" />

        <StackPanel Grid.Row="1" Margin="20,20,20,20">
            <TextBlock Margin="20,0,20,0" Text="Rechercher par client" />
            <TextBox Margin="20,0,20,0" />
        </StackPanel>

        <StackPanel
            Grid.Row="2"
            Width="960"
            Margin="0,0,0,20"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <ScrollViewer>
                <DataGrid
                    Width="862"
                    Height="468"
                    AutoGenerateColumns="False"
                    IsReadOnly="True"
                    ItemsSource="{Binding LesCommande}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Width="*" Header="Numéro de commande" Binding="{Binding Numcommande, ValidatesOnExceptions=True}"/>
                        <DataGridTextColumn Width="*" Header="Date de création" Binding="{Binding Datecommande, ValidatesOnExceptions=True}"/>
                        <DataGridTextColumn Width="*" Header="Vendeur" Binding="{Binding Numclient, ValidatesOnExceptions=True}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </ScrollViewer>
            <Button
                x:Name="butSuppr"
                Width="30"
                Height="30"
                Margin="30"
                Content="&#xE74D;"
                FontFamily="Segoe MDL2 Assets"
                FontSize="16"
                ToolTip="Supprimer" />

        </StackPanel>
    </Grid>
</UserControl>
